version: 2

models:
  - name: stg_customers
    description: One record per customer from the jaffle_shop source.
    columns:
      - name: customer_id
        description: The unique identifier for a customer.
        data_tests:
          - unique
          - not_null
